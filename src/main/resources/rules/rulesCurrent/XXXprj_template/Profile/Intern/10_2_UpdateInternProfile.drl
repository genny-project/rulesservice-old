package life.genny.rules;
import life.genny.rules.QRules;

rule "Update Intern Profile"
	when
	    rules: QRules( 	isState("QUE_INTERN_GRP") && 
						isState("EVENT_FORM_SUBMIT") && 		
						isState("PRI_IS_PROFILE_COMPLETED") && 
						!isState("LOOP_UPDATE_INTERN_PROFILE")) 
	 then
	 	rules.header();
        rules.setState("LOOP_UPDATE_INTERN_PROFILE");

		rules.setState("TRIGGER_PROFILE_CHECK");
	 	rules.footer();      
end