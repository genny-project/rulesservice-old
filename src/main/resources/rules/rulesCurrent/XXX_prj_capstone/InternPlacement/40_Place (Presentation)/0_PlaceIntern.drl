package life.genny.rules;
import life.genny.rules.QRules;

/* Set BEG \ APP \ INTERN To Map */
rule "Place Intern - Step 0"
	when
		rules: QRules( 	isState("BTN_PRESENTATION_INTERN") && 
						isState("EVENT_BTN_CLICK") && 
						!isState("LOOP_PLACE_INTERN_STEP0") ) 
	 then
	 	rules.header();
        rules.setState("LOOP_PLACE_INTERN_STEP0");

		String begCode = rules.getAsString("itemCode");

		if(begCode == null){
            rules.println("begCode is null");
			return;
        }else{
			BaseEntity beg = rules.getBaseEntityByCode(begCode);
			rules.set("begBe", beg);

			rules.setState("PLACE_INTERN_READY");            
			rules.setState("MOVE_BEG_TO_PLACED");
        }
	 	rules.footer();      
end